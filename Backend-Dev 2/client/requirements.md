## Packages
framer-motion | For smooth animations of transactions and balance changes
lucide-react | Beautiful icons for the interface (already in base, but explicit for clarity)
clsx | Conditional class merging
tailwind-merge | Utility for tailwind class merging

## Notes
The app tracks financial transactions.
API endpoints defined in @shared/routes.
Balance is derived from the transaction history on the server.
Undo/Redo logic is handled server-side.
