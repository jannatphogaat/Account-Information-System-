## Packages
framer-motion | Smooth animations for dialogs and list transitions
lucide-react | Beautiful icons for the dashboard

## Notes
- Using Google Fonts: 'Outfit' (Display) and 'DM Sans' (Body)
- API endpoints defined in shared/routes.ts are used for all transaction operations
- Simple client-side state manages the "Exit" simulation and "Show Records" toggle
